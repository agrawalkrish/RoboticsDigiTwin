Prerequisites & Installation
You will need Python installed (preferably 3.8+ and <3.14).

Install the required libraries using pip:
pip install gymnasium panda_gym stable-baselines3 shimmy

Then run train.py
After completing train.py run test.py
